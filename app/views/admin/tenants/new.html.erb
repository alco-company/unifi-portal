<% content_for :title, "New tenant" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New tenant</h1>

  <%= render "form", tenant: @tenant %>

  <%= link_to "Back to tenants", admin_tenants_path, class: "heimdall_button" %>
</div>
