<turbo-frame id="otp_input">
  <div class="p-4 border rounded bg-red-100 text-red-800">
    ❌ <%= @error %>
  </div>

  <%= button_to "Send ny kode",
      resend_otp_path,
      method: :post,
      remote: true,
      data: { turbo: true, resend_target: "button" },
      class: " text-blue-600 hover:underline" %>
  </div>
</turbo-frame>