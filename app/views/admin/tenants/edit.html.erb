<% content_for :title, "Editing tenant" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing tenant</h1>

  <%= render "form", tenant: @tenant %>

  <%= link_to "Show this tenant", admin_tenant_path(@tenant), class: "heimdall_button" %>
  <%= link_to "Back to tenants", admin_tenants_path, class: "heimdall_button" %>
</div>
